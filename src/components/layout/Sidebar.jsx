import React from 'react';
import { Link } from 'react-router-dom';

const Sidebar = () => {
    return (
        <Link to="/client/add">
            New
        </Link>
    );
};

export default Sidebar;
